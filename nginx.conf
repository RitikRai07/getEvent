# ==========================================================
# Nginx Config - TicketCharge Hub Frontend
# Handles: React SPA routing + API proxy to backend
# ==========================================================

server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # ---------- React SPA - handle all routes ----------
    location / {
        try_files $uri $uri/ /index.html;
    }

    # ---------- Proxy /api requests → backend container ----------
    location /api/ {
        proxy_pass         http://backend:5050/api/;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        'upgrade';
        proxy_cache_bypass $http_upgrade;
    }

    # ---------- Proxy uploaded images → backend container ----------
    location /uploads/ {
        proxy_pass http://backend:5050/uploads/;
    }

    # ---------- Cache static assets 1 year ----------
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # ---------- Security headers ----------
    add_header X-Frame-Options       "SAMEORIGIN"   always;
    add_header X-Content-Type-Options "nosniff"     always;
    add_header X-XSS-Protection      "1; mode=block" always;

    # ---------- Gzip ----------
    gzip on;
    gzip_vary on;
    gzip_min_length 1000;
    gzip_types text/plain text/css application/json application/javascript
               text/xml application/xml application/xml+rss text/javascript
               image/svg+xml;
}
